function Panel({
    nodes,
    addNewPanel,
    openData,
}: {
    nodes: any[];
    addNewPanel: ({ node }: { node: string }) => void;
    openData: (data: any) => void;
}) {
    return <div></div>;
}

export default function Navigator() {
    return <div></div>;
}
